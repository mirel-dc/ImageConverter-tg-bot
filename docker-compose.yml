version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app/src
    env_file:
      - .env
    command: ["uv", "run", "python", "-m", "bot.bot"]
    volumes:
      - ./:/app
    restart: unless-stopped
